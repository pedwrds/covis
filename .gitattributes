# Normalise line endings of text files to LF for this repo (overrides
# contributor's autocrlf setting).
* text=auto
# Declare files that will always have LF line endings on checkout.
*.sh text eol=lf
# Declare files that will always have CRLF line endings on checkout.
*.bat text eol=crlf
# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
lfs/ filter=lfs diff=lfs merge=lfs -text
lfs/choro_slider.html filter=lfs diff=lfs merge=lfs -text
lfs/choro_slider_test.html filter=lfs diff=lfs merge=lfs -text
